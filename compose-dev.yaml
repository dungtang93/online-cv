services:
  app:
    entrypoint:
    - sleep
    - infinity
    build: .
    ports:
      - 4000:4000
    init: true
    volumes:
    - type: bind
      source: /var/run/docker.sock
      target: /var/run/docker.sock

